{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "rewrites": [{
    "source": "**",
    "destination": "/index.html"
  }]
}
